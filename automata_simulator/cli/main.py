"""CLI entry point. Stub to be filled out in Stage 8."""

from __future__ import annotations

import sys


def main() -> int:
    """Entry point for the ``automata`` command.

    Summary (UA): Заглушка CLI — буде реалізовано на Етапі 8.
    Summary (EN): CLI stub — scheduled for implementation in Stage 8.

    Returns:
        Process exit code.
    """
    sys.stdout.write("automata CLI — not yet implemented (see Stage 8 in CLAUDE.md)\n")
    return 0


if __name__ == "__main__":  # pragma: no cover
    raise SystemExit(main())
